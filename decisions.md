# 決定事項 (Architecture Decisions)

アーキテクチャレベルで実装に影響する決定を記録する。各エントリは軽量ADR形式
（背景 / 決定 / 根拠 / 影響）。未確定の論点は `tbd.md` を参照。

---

## D-01: 言語は TypeScript

- 背景: `docs/system.md` がクリーンアーキテクチャ・80%カバレッジ・結合/E2Eを要求。
- 決定: 実装言語は **TypeScript**。
- 根拠: ドメイン/サービス層を型で保護でき、層境界（インターフェース）を明示できる。
  vitest/Playwright/Vite と親和性が高い。
- 影響: ビルドに型チェックを含める。全層で型定義を整備。

## D-02: ビルド/開発サーバは Vite

- 背景: 全てJS/HTML層で完結、静的アセットとして Apache に配信する要求。
- 決定: **Vite** を採用。
- 根拠: vitest と統合容易、`vite build` で静的アセット（`dist/`）を生成でき本番要件に合致。
- 影響: 設定は `vite.config.ts`。E2Eはビルド/プレビュー or dev サーバに対して実行。

## D-03: クリーンアーキテクチャの層構成

- 背景: ゲームロジックとUIの分離（サービス層）が要求。
- 決定: **domain / application(サービス層) / infrastructure / presentation** の4層、依存は内向き。
- 根拠: ゲーム規則を domain の純粋関数に閉じ込め、UIは状態の写像に徹することで、
  ゲームデザイン拡張がUIに引きずられない。
- 影響: presentation は `GameService` だけに依存。domain は外部ライブラリ非依存・副作用なし。

## D-04: 六角座標系は pointy-top / axial(+cube)

- 背景: AGENTS.md が Hexagonal Point Top を指定。
- 決定: 内部座標は **axial `(q,r)`**、距離・回転は **cube** に変換して計算。pointy-top のピクセル変換式を使用。
- 根拠: axial は格納が簡潔、cube は距離・近傍計算が明快。実績ある定式（redblobgames 系）に準拠。
- 影響: `domain/hex` に純粋関数として集約し、ユニットテストで網羅。

## D-05: マップ描画は Canvas 2D（確定）

- 背景: 多数の六角セルの描画とインタラクション。
- 決定: **Canvas 2D** で hex グリッドを描画、HUDはDOMオーバーレイ（指示者判断 T-02 で確定）。
- 根拠: 多セル描画・再描画・アニメーションに有利。pixel↔hex 変換で入力を扱える。
- 影響: presentation層のみ。**敵/ロボット数の増大で1ターンの描画/計算負荷が上がった場合の方式**（WebGL 等）は
  計測してから判断する将来論点として `tbd.md` T-04 に残す（描画と計算のどちらがボトルネックかで結論が変わるため）。

## D-06: 決定的乱数（SeededRng）の全面採用

- 背景: テスト安定化・バグ再現・デバッグ容易化。
- 決定: マップ生成・敵AIなど確率的処理はすべて**注入された `SeededRng`** を経由する。
- 根拠: 同シードで局面を完全再現でき、結合テストとQAが安定する。
- 影響: domain/application は乱数を直接生成せず注入で受け取る（純粋性の維持）。

## D-07: ローカル本番相当確認は Apache(httpd) コンテナ（確定）

- 背景: デプロイ先が Ubuntu 24.04 + Apache 2。指示者判断 T-03 で「Docker Compose 環境で一貫して動作確認し、
  ローカルとデプロイ先（特に Vite ローカルサーバ周り）の差異を避けたい」と確定。
- 決定: Docker Compose に `dev`(Vite) と `web`(httpd:2.4 で `dist/` 配信) を用意し profiles で切替。
- 根拠: 本番（Apache）に近い静的配信をローカルで再現でき、乖離を早期に発見できる。
- 影響: Compose 構成・`deploy/` の設定例。
- **作業順の方針**: 指示者は「Phase 7 まで Compose 準備が遅れる計画」を懸念しており、
  デプロイ先環境とシームレスに結びつけるため Compose 整備を**前倒しできるなら前倒しする**ことが望ましい
  （MVP ループ確認・E2E をできるだけ本番相当の `web` 配信上でも行えるようにする）。`todos.md` 冒頭参照。

## D-08: GameState はシリアライズ可能な純データ

- 背景: セーブ/ロード・結合テスト・再現性。
- 決定: `GameState` を関数やクラスインスタンスを持たない**純データ**として設計。
- 根拠: スナップショット比較・永続化・再現が容易になり、サービス層の検証が単純化する。
- 影響: 振る舞いは domain/application の関数側に置き、状態はデータに保つ。
  ターン内の残移動力・行動済みは `turnState` として GameState 内に保持する。

## D-09: ドメインイベントの配信経路は subscribe に一本化

- 背景: `GameService.dispatch` の戻り値と `subscribe` の双方からイベントが流れると、敵相（プレイヤーの
  コマンドを伴わない自動進行）のイベントの扱いが曖昧になり、UI側で二重適用バグを招く。
- 決定: **全ドメインイベントは `subscribe` 経由でのみ配信**。`dispatch` の戻り値（`CommandResult`）は
  適用の**成否と拒否理由のみ**を返し、イベントは含めない。
- 根拠: 配信経路を単一化することで、敵相を含む全イベントを一貫した順序で扱え、重複適用を防げる。
- 影響: UIは subscribe で差分更新、または getState() で全体再描画する。`EndTurn` 起因の敵相イベントも
  すべて subscribe から流れる。

## D-10: カバレッジ閾値の対象範囲

- 背景: `docs/system.md` の「カバレッジ80%以上」をどの範囲に課すか。presentation を含めると E2E 分が
  Vitest のカバレッジに算入されず未達になりやすい。
- 決定: Vitest coverage の **include は `src/domain` と `src/application`**。閾値80%はこの範囲に強制する。
  `src/presentation`・`src/infrastructure` のアダプタは閾値対象外とし **E2E（Playwright）で担保**する。
- 根拠: ゲーム規則の中核（domain/application）を確実に高カバレッジで守りつつ、UI起因のCI赤化を避ける。
- 影響: `vitest.config.ts` の coverage include/exclude 設定。E2Eで presentation の主要経路を網羅する。

## D-11: 決定性の規律（生成・敵相・再試行）

- 背景: D-06 の「同シード→同結果」を、マップ生成のリトライや敵相の複数ユニット処理にまで一貫させる必要。
- 決定:
  - マップ生成の到達可能性リトライは **seed を派生させず同一 RNG ストリームを継続消費**し、上限超過時は
    決定的フォールバック（通路を彫る）を適用する。
  - 敵相は **`id` 昇順**で処理し、同距離の対象/方向は**近傍6方向の固定優先順位**でタイブレークする。
- 根拠: 「同シード・同コマンド列 → 同 GameState」をゴールデンテストで検証可能にする。
- 影響: MapGenerator・敵AI・結合テスト（決定性ゴールデンテスト）。

## D-12: ユニットモデル — 可動ユニットと固定建造物の分離（指示者判断 G-02/G-05/G-08）

- 背景: 「プレイヤー本体」「脱出ポッド」「ゴール（宇宙船）」「巣」「ロボット」「敵」をどうモデル化するか。
  特に「1タイルに最大1ユニット」という基本則と、プレイヤーがポッド上に重なれる要求が衝突する。
- 決定:
  - ユニットを **可動ユニット（movable: player / robot / enemy）** と **固定建造物（structure: pod / goal / nest）** に
    概念分離する。「1タイル最大1**可動**ユニット」を基本則とし、**固定建造物の上には可動ユニットが重なれる**。
  - **脱出ポッドは開始タイルの固定建造物**で、**HP/防御を持ち破壊され得る**（能動操作はしない＝「動作しないユニット」とみなしてよいが、
    可動ユニットが重なれることは必須）。プレイヤー本体は可動ユニットで、開始時はポッド上または近傍に置く。
  - **敗北条件は「プレイヤー本体の死亡」または「ポッドの破壊」のいずれか**（従来の「プレイヤー死亡のみ」を改める）。
  - プレイヤーがポッドに重なっている状態で攻撃を受けた場合、**ダメージはプレイヤーHPではなくポッドHPに入る**。
  - 敵は視界内のポッド・プレイヤー双方を攻撃し得る。**敵の攻撃優先度は プレイヤー > ポッド(基地) > ロボット**
    （将来この優先度を上書きする存在＝デコイ等はあり得る）。
- 根拠: 物語上「脱出ポッド＝拠点」を中核に据えつつ、移動・建造・重なり・敗北を一貫したルールで表現できる。
- 影響: ユニット/建造物の型分離、`GameState`（structures と units）、移動の占有判定（可動のみブロック）、
  戦闘のダメージ宛先（重なり時はポッド）、敗北判定（`evaluateStatus` に `podAlive` を追加）、マップ生成の pod 配置、
  建造（pod 上のプレイヤー本体のみ）。ポッドの HP/防御の数値は `rule.md §7.1` の暫定値（プレイ感で調整）。

## D-13: 決定的予見可能ポリシー（イベントRNGの設計指針）

- 背景: ターン制戦闘（Slay the Spire 等に類似）で、プレイヤーが行動を確定する前に、そのターンの敵・他ロボットの動作や
  巣からの生成などの結果を **100%予見できる**ことを将来にわたり保証したい。同時に「謎のほこら（HPを失い、選べない
  ランダムな正効果を1つ得る。いつ実行してもそのゲーム内では同結果）」のように、**実行タイミングに依らず結果が決定的**な
  イベントを成立させたい。
- 決定:
  - ユーザーの決定が同じならゲーム開始から終了まで挙動は**決定的**であり、各ターンのユーザー決定後の結果（移動先・被ダメ等）は
    確定前に正確に提示できることを設計原則とする（UI ではダメージ予測・移動先プレビュー等で可視化することが望ましい）。
  - これを満たすため、**単一 RNG ストリームを順に消費するだけでは不十分**な場合がある（消費順＝実行順に結果が依存してしまう）。
    将来のイベント結果は、**イベント固有のキー（ゲームシード＋イベント識別子）から派生した部分ストリーム**で決めるなど、
    「いつ引いても同じ結果」を保てる引き方を採用する。これは D-06/D-11 を将来のイベント系に向けて精緻化する位置づけ。
- 根拠: 予見可能性は本作のコア体験。RNG の引き方を後から変えると再現性・セーブ互換が壊れるため早期に方針を固定する。
- 影響: 将来のイベント/効果システムの RNG 設計。MVP のマップ生成・敵AI（実行順依存で問題ない範囲）は現行の単一ストリームのままでよい。

## D-14: 将来拡張の確定方針（クエスト／ロボット自律行動／攻撃種別）

MVP では実装しないが、後続設計の前提として確定した方向性（指示者判断 G-01/G-03/G-07/G-08）。

- **クエスト構造（G-01/G-08）**: クリア条件を満たすために**複数のクエストをスタックできる構造**とする。
  各クエストは「背景 / 達成条件 / 達成の報奨 / 達成前後の説明・フレーバーテキスト」を持つ。宇宙船の「修理」は
  2〜3 の**必須クエスト**として表現する。MVP のクリア条件は「ロケット（宇宙船）に到達」のままでよく、
  「クリア条件＋それを満たす1つ以上のクエスト」というモデリングを将来の土台とする。
- **ロボットの自律行動（G-07）**: ロボットは「待機」「実行中」の状態を持ち、実行中は**有限パターンの固定的で簡潔な
  アルゴリズム**で動く。選択できるアルゴリズムはロボットごとに決まる（例: 指定地点へ最短に見える経路で移動／視界内の敵へ接近攻撃／
  視界内の敵へ砲撃／敵から逃げる）。「実行中→待機」に戻せるか自体もロボットの性能（初期ロボットは敵対行動を止められない等）。
- **攻撃種別（G-03）**: ユニットは HP/視界/移動力/攻撃に加え**攻撃の種類**を持つ。指定なき MVP は「近接」。
  既定の方向性として、プレイヤーは近接、**ポッドに重なっているプレイヤーは距離2まで対応可（防衛力向上）**。
  ロボットは近接・遠隔のほか「Xターン後に対象範囲（半径）へ所定ダメージ」のような**予見可能で持続的な動作**もあり得る。
- 影響: 将来の domain/rules（combat の射程・攻撃種別）、application（ロボット行動エンジン）、quest モデル、UI（予見表示）。

## D-15: UIフレームワークは必要時に導入（指示者判断 T-01）

- 背景: HUD が将来複雑化した場合に素の TS + DOM のままでよいか。
- 決定: **MVP は素の TS + DOM**。必要になった具体的状況に応じて導入を判断する。指示者の好み（参考）は、
  「Web フレームワーク」を使うなら **Vue.js** が第一候補、次いで preact / React(SPA)。Web Component 系の軽量フレームワークなら
  やや **Lit** を選好。いずれを採るにせよ、結合度の濃淡に応じた**多層テスト（ユニット/結合/E2E/総合）**で頑強性を担保することを重視する。
- 根拠: ゲームである本作では「Web フレームワーク」の利点が効きにくい可能性が高く、MVP では非依存が単純で安全。
- 影響: presentation 層の実装方針のみ。domain/application には影響しない。

## D-16: アンドゥと敵相プレビュー（決定的予見可能性の操作実装・MVP最終目標／指示者要望）

- 背景: D-13 の決定的予見可能ポリシーを**操作面で実現**する。プレイヤーがそのターンの行動を確定する前に、敵・他ロボットの
  そのターンの行動と結果まで予見でき、行動を修正（アンドゥ）できることが MVP 最終時点で望ましい（指示者要望）。
  現行のターン構造は「プレイヤー相 → EndTurn 後に敵相」のため、確定前に敵の動きを見るには下記2点の追加が要る。
- 決定:
  - **アンドゥ**: `GameService` がプレイヤー相の各コマンド適用直前の `GameState` スナップショットをスタックで保持し、
    `undo()` で直前状態へ戻す。スタックは敵相開始（EndTurn 適用）でクリアする（＝アンドゥはそのターンのプレイヤー相内に限る）。
    `GameState` は純データ（D-08）なので `structuredClone` で安価に巻き戻せる。Command 体系は増やさず UI 操作として扱う。
  - **敵相プレビュー**: 敵相を「**試算（純粋・emit なし、clone 上で敵AI/ターンエンジンを走らせ予測のみ返す）**」と
    「適用（emit あり、現行）」に分離する。現在のプレイヤー側配置から試算し、敵の移動先・被ダメ予測を UI に表示する。
    決定的（D-06/D-11）なので予測は実結果と一致する。プレイヤーの暫定配置が変わるたびに再試算する。
- 根拠: 予見可能性は本作のコア体験（D-13）。既存アーキテクチャ（決定的・純データ・敵相が副作用を emit に限定）がこれを安価に支える。
- 影響: application（`GameService.undo()`／敵相試算の純関数化＝emit 分離）、presentation（プレビュー描画・Undo 操作）。
  `todos.md` の「予見可能性タスク（MVP最終目標）」に分解。プレビューの表示粒度（移動先のみ／被ダメ込み 等）は実装時に調整。

## D-17: UI 実装方式は素の TS + DOM を継続（D-15 の具体適用・指示者判断）

- 背景: マウス操作の行動メニュー・左右下のユニット一覧パネル・マップ重畳オーバーレイの追加（タスク `20260621_016`）で
  HUD の複雑度が一段上がる。D-15 の「UI フレームワークは**必要になった具体的状況で判断**」に該当する判断時点となった。
- 決定: **素の TS + DOM を継続**する。本作は Canvas ベースで、ゲーム本体の描画/入力は Canvas が担い、DOM 側は
  情報パネル＋コンテキストメニュー＋ボタンの小規模に留まる。presentation を CanvasRenderer / Hud(情報パネル) /
  ActionMenu / InputController の小モジュールに分割し、将来 DOM 側が肥大化（クエスト/インベントリ/複数メニュー等）した
  時点でフレームワーク（D-15 の Vue/Lit）導入を再判断する。
- 根拠: フレームワークの主な旨味（複雑な双方向状態の宣言的記述）はゲーム本体＝Canvas には効かず、Canvas の中身は
  フレームワーク管理外。DOM 側は `GameState` スナップショット＋クエリからの**一方向再描画**で足りる規模であり、
  導入のビルド/依存/テスト構成の追加コストに見合わない。
- 影響: presentation 層のみ。選択状態（`own`/`enemy`/`none`・design.md §12.2）は presentation が UI 状態として保持し、
  `GameState` には入れない（規則に影響しないため）。
