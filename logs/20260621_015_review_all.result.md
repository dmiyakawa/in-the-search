# 20260621_015 全実装完了後の全体レビュー（result）

- 実施日時: 2026-06-21 20:55 JST
- 使用モデル: Claude Opus 4.8
- 工程: review

## 対象・観点

`todos.md` の全タスク完了（Phase 0–8 + G-02反映 + 予見可能性 + Phase 2レビュー指摘）を受け、
関連文書（`docs/rule.md` / `design.md` / `decisions.md`）との整合、設計意図と実装の一致、
全レイヤのテスト成功（警告残存含む）を確認した。未コミット差分（`20260621_014` 由来）が対象。

## テスト結果（全レイヤ green・警告なし）

- `npm run format:check`: pass（Prettier 全件適合）
- `npm run lint`: pass（ESLint 指摘なし）
- `npm run build`: pass（`tsc --noEmit` + `vite build` 成功）
- `npm run test:cov`: pass（18 files / 104 tests）。カバレッジ **All 96.63%**、application 96.16%、
  domain 各 100% 近傍。D-10 閾値 80% を全領域で上回る。
- `npm run e2e`: pass（chromium 2 tests, 2.3m）。前回 result で言及の NO_COLOR/FORCE_COLOR 警告は
  今回出力に**残存せず**。

## 確認した整合性（問題なし）

- **rule.md ↔ 実装**: 勝敗優先（goal > 死亡/pod破壊）、敵攻撃優先度 player>pod>robot、scout建造
  （pod上のプレイヤー本体のみ・隣接空きへ配置・当ターン行動済み）は実装・テストと一致。
  rule.md §7 に scout 配置/次ターン操作の補足が追記済み。
- **decisions.md（D-11 決定性）**: 乱数は注入 `SeededRng` のみ、敵相は id 昇順→`HEX_DIRECTIONS` index 順。
  `determinism.test.ts` で同 seed・同コマンド列の再現性を担保。
- **層構成**: presentation→`GameService` 依存、domain 純粋関数が維持されている。
- **Phase 2 レビュー指摘の解消**: 二桁id順序を `idRank`/`compareRank` の自然順比較に修正（`e2 < e10`）、
  `dispatch` default を `assertNever` 化。いずれもユニットテスト追加済み。
- **tbd.md**: 関連決定番号の誤記（D-15→D-14）を修正済み。

## 軽微な所見（任意改善・今回は未修正／残作業化しない）

いずれも欠陥ではなく設計上のトレードオフ。MVP として許容範囲のため修正せず記録に留める。

1. **決定性テストが run-vs-run 等価比較**（`tests/integration/determinism.test.ts`）。
   todos が例示した `toMatchSnapshot`（凍結ゴールデン）ではなく2回実行の一致で検証している。
   再現性（D-11）は担保されるが、コード変更による静かな出力ドリフトの検知力は一段弱い。将来 golden 化を検討。
2. **E2E のテスト専用足場が本番バンドルに含まれる**（`src/main.ts` の `createE2eWinState` / `window.__ITS_TEST__`）。
   `?__scenario=win` / `?__test` のクエリでゲートされ小規模なため許容。E2E 安定化目的として妥当。
3. **`smoke.spec.ts` の `hexToPixel` がレンダラの式を複製**。式が分岐すると追従漏れの可能性。
   現状 `hex-reference` と一致。将来テスト用に共有する余地あり。

## 結論

大きな問題（仕様取りこぼし・層逸脱・決定性破れ・テスト不足）はなし。残作業として切り出すべき項目もなし。
未コミット差分（`20260621_014` の Phase 6/8/レビュー対応）はガイドラインに従いコミットする。
