# 20260621_005 review: Phase 2 までのレビュー

- 実施日時: 2026-06-21
- 使用モデル: Claude Opus 4.8
- 工程: review（its-review）
- 対象: Phase 0〜2 の差分（`git diff ef37bfa..HEAD`）。主対象は Phase 2（application 層）。
- 照合した正典: `docs/rule.md` / `design.md` / `decisions.md`（D-09/D-10/D-11）/ `todos.md` 完了条件 /
  `docs/hex-reference.md` / `docs/types-reference.md`。
- テスト状況: `npx vitest run` → 12 ファイル 67 テスト green。

## 総評

層構成（domain 純粋・application が調整役・presentation 未着手）と依存方向は設計どおり。
決定性の規律（D-11: 乱数注入・`HEX_DIRECTIONS` index 順タイブレーク・`id` 昇順）も `enemyAi`/`turnEngine`
で守られている。イベントは `subscribe` 一本化（D-09）、`getState` は `structuredClone`（D-08 のスナップショット性）で適正。
hex 実装は `hex-reference` を一字一句踏襲（`-0` 正規化含む）。型・定数は `types-reference §5` に集約済み。
おおむね良好。下記の指摘を `todos.md`（必須/軽微）と `tbd.md`（仕様判断）に記録した。

## 指摘（重大度別）

### 必須
- **`AttackUnit` のフレンドリーファイア** — `src/application/GameService.ts:137-144`。対象が自軍ユニット（player/robot）でも
  攻撃が成立する（`isPlayerSide` 判定欠落）。`rule.md §6`「敵を攻撃する」に反する。`target-not-enemy` の拒否理由が
  用意済みだが「対象不在」にしか使われていない。現状は自ユニットが player 1体のみで顕在化しないが、**Phase 5 のロボット導入で live**。
  → 自軍ユニット・自身を `target-not-enemy` で拒否し、テスト追加。

### 望ましい
- **敵接近の vision 制限の乖離** — `src/application/turn/enemyAi.ts:20`。実装は `distance<=vision` に限定（design/todos の意図どおり）だが
  `rule.md §6` は視界制限を明記せず、正典と食い違う。仕様判断を `tbd.md G-05a` に記録（暫定は現行維持、確定後 rule.md 追記）。

### 軽微
- **二桁 id のソート順** — `turnEngine.ts:20` `.sort()` / `util.ts` `minBy` は文字列辞書順。`ENEMY_COUNT=5` の現状は無問題
  （`types-reference §6` が前提を明記）。将来二桁化時はゼロ埋め採番等で対処。
- **`dispatch` の到達不能 default** — `GameService.ts:88` `return cmd;`。`assertNever` 等の方が意図明確。
- **`GatherResource`/`BuildRobot` が `{ ok:true }` の no-op** — `GameService.ts:81-86`。todos どおり Phase 5 保留の意図的措置だが、
  Phase 4 の UI 配線時に「成功扱い」を誤解しないよう Phase 5 で正式実装する点を留意。

## consolidate への引き継ぎ（設計⇄実装の乖離）
- `rule.md §6` の敵接近に「視界内に限る」旨を追記（G-05a 確定後）。それ以外に design.md / 正典との重大な乖離は検出されず。

## 確認した良い点（萎縮回避のため明記）
- `checkMove` の拒否順（隣接→境界→地形→占有）と reason 名が `rule.md §5/§6`・`commands.ts` と一致。
- `updateVisibility` の visible→discovered 降格→自ユニット視界 visible 化、`nowVisible` 算出が `rule.md §3` どおり。
- `evaluateStatus` の勝利優先（`rule.md §4.1`）。`handleMoveUnit` でゴール到達=即 won、敵相スキップも適正。
- `turnEngine` が敵 id 列をループ前確定・各反復で生存確認・敗北判定のみ・`turnState` を自ユニットから再構築（`types-reference §6` の規約どおり）。
